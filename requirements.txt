# Core dependencies
numpy>=1.26.0
scipy>=1.15.0
pandas>=2.2.0
matplotlib>=3.7.0
tqdm>=4.67.0
psutil>=7.0.0
pyyaml>=6.0
scikit-learn>=1.0.0
h5py
aiohttp

# GPU computing
cupy-cuda12x>=13.0.0
cuvs-cu12

# RAPIDS ecosystem (CUDA 12)
cuml-cu12>=25.4.0
rmm-cu12>=25.4.0

# Vector search libraries
faiss-cpu
# faiss-gpu  # Use faiss-cpu if no GPU available
# cuvs>=23.10.0  # Note: cuVS typically installed via conda

# Machine learning and optimization
optuna>=2.10.0
seaborn>=0.12.0

# Networking and communication
grpcio>=1.40.0
grpcio-tools>=1.40.0
protobuf>=3.19.0

# Jupyter and notebook support
jupyter>=1.0.0
notebook>=6.4.0

# Hugging Face and model serving
huggingface_hub
tritonclient[all]

# GPU monitoring
pynvml

# Parallel processing (built-in, but listing for clarity)
# concurrent.futures - part of Python standard library

# Development/logging
# logging - part of Python standard library
# os, gc, time, itertools, collections - part of Python standard library
